<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Rupiyah" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <!-- Soft activity transitions (widget open/close + general polish). -->
        <item name="android:windowAnimationStyle">@style/Animation.Rupiyah.Activity</item>
    </style>

    <style name="Animation.Rupiyah.Activity" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/widget_open_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/widget_open_exit</item>
        <item name="android:activityCloseEnterAnimation">@anim/widget_close_enter</item>
        <item name="android:activityCloseExitAnimation">@anim/widget_close_exit</item>
    </style>
</resources>
