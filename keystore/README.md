# Keystores

Place your release `.jks` / `.keystore` files here.

- **Do not commit** real keystores or passwords.
- Copy [`../keystore.properties.example`](../keystore.properties.example) to `../keystore.properties` and fill in values.
- This directory is listed in `.gitignore` for `*.jks` and `*.keystore`.
